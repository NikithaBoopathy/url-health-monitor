apiVersion: v1
kind: Service
metadata:
  name: monitor-service
  namespace: monitoring
spec:
  selector:
    app: url-health-monitor
  type: NodePort
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30800
